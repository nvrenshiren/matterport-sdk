import * as ss from "./73085"
import { TourMode } from "../const/tour.const"
import { ToursViewData } from "../data/tours.view.data"
const r = (0, ss.M)(ToursViewData, "currentTourMode", TourMode.NONE)

export const S = r
