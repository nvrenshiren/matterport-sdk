import * as i from "./73085"
import * as s from "./74554"
import { ToolsData } from "../data/tools.data"
const a = (0, i.M)(ToolsData, "activeToolName", null)
;(0, s.M)("activeTool", a)

export const B = a
