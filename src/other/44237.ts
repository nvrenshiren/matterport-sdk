const i = (e, t, n = 0) => {
    const i = [0, 0, 0, n]
    return t.toArray(i), e.fromArray(i), e
  },
  s = e => !e || !Object.values(e).filter(e => 0 !== e).length
export const Ot = i
export const mB = s
