export enum TourAddPosition {
  Beginning = 0,
  Current = 1,
  End = 2
}
export enum TourState {
  Active = 1,
  Inactive = 0,
  StopScheduled = 2
}
export enum TourMode {
  NONE = "none",
  STORIES = "stories",
  LEGACY = "legacy"
}
